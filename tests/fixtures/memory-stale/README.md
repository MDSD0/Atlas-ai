# Memory Stale Fixture

Seed a linked LocalRecords fact that the greeting ends with a period. Change
`src/greeting.ts`, then assert that the linked record becomes stale and default
recall excludes it.
